import React from 'react';

export default function Dashboard({ user }) {
    return (
        <React.Fragment>
            <h1>Dashboard</h1>
            <p>Features to be added..</p>
        </React.Fragment>
    );
}